<%= form_with(url: verify_demo_path, method: :post, local: true) do |form| %>
  <%= label_tag(:name, "What's your name ? ") %>
  <%= text_field_tag(:name) %>

  <%= hcaptcha_tag %>

  <%= submit_tag('Do something magic!') %>
<% end %>
